import { CrearRepresentanteDirective } from './crear-representante.directive';

describe('CrearRepresentanteDirective', () => {
  it('should create an instance', () => {
    const directive = new CrearRepresentanteDirective();
    expect(directive).toBeTruthy();
  });
});
